# MEMORY（ループの長期記憶）

モデルは忘れる。リポジトリは忘れない。
ループが毎ラウンド最初に読む「踏んだ罠」の置き場。1行 = 1つの教訓（症状 → 原因 → 対策）。

## 罠ログ
- 症状: `node --test` が「0 tests」で緑になる → 原因: import 先のパス間違いでテストが読まれていない → 対策: 実行後に必ず pass 件数を確認する（件数 0 は赤と同じ扱い）。

## プロジェクトの固定事実
- ランタイム: Node.js（依存ゼロ。`node --test` だけで回る）
- 受け入れ条件は `test/` に置く。ループは `test/` を絶対に編集しない。
- PR は必ず Draft。`main` へ直接コミットしない。
